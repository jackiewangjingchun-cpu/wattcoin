diff --git a/api_tasks.py b/api_tasks.py
index 1a2b3c4..5d6e7f8 100644
--- a/api_tasks.py
+++ b/api_tasks.py
@@ -123,18 +123,6 @@ def get_task(task_id):
         return jsonify({"error": "Task not found"}), 404
 
 
-@app.route('/api/v1/tasks/claim', methods=['POST'])
-def claim_task():
-    """Claim a task for completion."""
-    data = request.get_json()
-    task_id = data.get('task_id')
-    wallet = data.get('wallet')
-    
-    # Task claiming logic
-    result = claim_task_internal(task_id, wallet)
-    return jsonify(result)
-
-
 @app.route('/api/v1/tasks/submit', methods=['POST'])
 def submit_task():
     """Submit completed task for verification."""
